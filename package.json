{
  "name": "ship-happens",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ship Happens — a startup-survival tower defense game.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "jsdom": "^30.0.1",
    "vitest": "^5.0.0"
  }
}
